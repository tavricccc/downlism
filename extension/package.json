{
  "name": "downlism-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Hands Chrome and Edge downloads to Downlism.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
